'use client';

import { styled } from '@/libs/stitches';

export const Wrapper = styled('div', {
  minHeight: '100vh',
  position: 'relative',
  zIndex: 1,
  display: 'flex',
  flexDirection: 'column',
});
